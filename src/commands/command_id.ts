export namespace DiscordCommandId {
  export enum Txt2Img {
    get = 'get',
    post = 'post',
    slash = 'imagine',
    modal = 'modal',
    prompt = 'prompt',
    retry = 'retry',
    original = 'original',
    random = 'random',
    negative_prompt = 'negative_prompt',
  }
}
